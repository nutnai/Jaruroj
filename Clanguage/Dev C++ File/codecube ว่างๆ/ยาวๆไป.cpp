#include <stdio.h>
#include <string.h>

int n,x,y,z,m;
char y=[YES],n=[NO];

main(){
	scanf("%d",&n);
	for(int i;i<=n;i++){
		scanf("%d%d%d",&x,&y,&z);
		m=(x>=y)? ((x>=z)? x:z):((y>=z)? y:z);
		if(x!=0){
			if(y==0)
		}
	}
	
}
